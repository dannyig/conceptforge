import React from 'react'

// Root component — stub only
// Canvas Agent will replace this with the full app layout
export function App(): React.JSX.Element {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
        backgroundColor: '#0d1117',
        color: '#e6edf3',
        fontFamily: 'monospace',
        fontSize: '24px',
      }}
    >
      ConceptForge
    </div>
  )
}
